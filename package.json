{
  "name": "the-rotation",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node server.mjs",
    "build": "rm -rf dist && cp -r site dist",
    "build:dev": "rm -rf dist && cp -r site dist"
  }
}
