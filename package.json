{
  "name": "the-rotation",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python3 -m http.server 8080 --directory site --bind 0.0.0.0",
    "build": "rm -rf dist && cp -r site dist",
    "build:dev": "rm -rf dist && cp -r site dist"
  }
}
